import React from 'react';

function Header(props) {
    return (
        <div style={{marginTop:20}}>
            <h1 className={`ui header`}>Tipping Calculator</h1>
        </div>
    );
}

export default Header;